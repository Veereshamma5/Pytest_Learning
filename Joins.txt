Part 2: Joins
Question 3: Inner Joins
Retrieve the names of students along with the names of the courses they are enrolled in.

Get the names of students, their departments, and the courses they are enrolled in.
==========================

select concat(s.firstname, ' ', s.lastname) as StudentName, c.coursename
from students s
JOIN enrollments e
ON s.studentid=e.studentid
JOIN courses c
ON e.courseid=c.courseid


select concat(s.firstname, ' ', s.lastname) as StudentName, d.departmentname, c.coursename
from students s
JOIN enrollments e
ON s.studentid=e.studentid
JOIN courses c
ON e.courseid=c.courseid
JOIN departments d
ON s.departmentid= d.departmentid
==============================================================================
Question 4: Outer Joins
Retrieve the names of all students and the courses they are enrolled in, including students without enrollments.

List all courses along with the names of students enrolled in each course, including courses without students.
===================================
select concat(s.firstname, ' ', s.lastname) as StudentName, c.coursename
from students s
LEFT JOIN enrollments e
ON s.studentid=e.studentid
LEFT JOIN courses c
ON e.courseid=c.courseid

========================================
select concat(s.firstname, ' ', s.lastname) as StudentName, e.courseid, c.coursename
from students s
LEFT JOIN enrollments e
ON s.studentid=e.studentid
LEFT JOIN courses c
ON e.courseid=c.courseid